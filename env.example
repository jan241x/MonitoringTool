# 基礎 URI（二選一：直接填完整 URI，或分別填 BASE_URL 與 PATH）
# Basic URI (choose one: full URI, or BASE_URL + PATH)
# HMAC_URI=https://apidev-extgw.clp.com.hk/transform

# 或分開配置 / Or configure separately
HMAC_BASE_URL=https://apidev-extgw.clp.com.hk
HMAC_PATH=/transform

# HTTP 方法 / HTTP method
HMAC_METHOD=POST

# 應用認證資訊 / Application credentials
HMAC_APP_KEY=your-app-key
HMAC_APP_SECRET=your-app-secret

# 可選：覆蓋請求標頭 / Optional: override request headers
# HMAC_ACCEPT=application/json; charset=utf-8
# HMAC_CONTENT_TYPE=application/x-www-form-urlencoded; charset=utf-8

# 查詢參數（urlencoded：key=value&key2=value2；留空則無）
# Query params (urlencoded: key=value&key2=value2; empty for none)
HMAC_QUERY=

# 請求體參數（根據 Content-Type 自動處理）
# Request body params (auto-handled based on Content-Type)
# 表單格式：key=value&key2=value2（當 Content-Type 為 application/x-www-form-urlencoded）
# Form format: key=value&key2=value2 (when Content-Type is application/x-www-form-urlencoded)
# JSON 格式：key=value&key2=value2（當 Content-Type 為 application/json 時會轉換為 JSON）
# JSON format: key=value&key2=value2 (converted to JSON when Content-Type is application/json)
HMAC_BODY=username=xiaoming&password=123456789

# 範例：JSON 請求 / Example: JSON request
# HMAC_CONTENT_TYPE=application/json
# HMAC_BODY=username=xiaoming&password=123456789
# 將產生：{"username":"xiaoming","password":"123456789"}
# Will produce: {"username":"xiaoming","password":"123456789"}

# 範例：直接 JSON 格式 / Example: Direct JSON format
# HMAC_CONTENT_TYPE=application/json
# HMAC_BODY={"username":"xiaoming","password":"123456789","age":25}
# 直接使用 JSON 字串 / Direct JSON string usage
